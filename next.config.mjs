/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['firebasestorage.googleapis.com', 'flauc.fudan.edu.cn', 'groundzero.fr', 'i.pinimg.com'],
      },
};

export default nextConfig;
